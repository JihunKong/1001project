# Docker Local Testing Environment
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:8001

# Docker Environment Enforcement (automatically satisfied in Docker)
DOCKER_REQUIRED=true

# NextAuth Configuration
NEXTAUTH_URL=http://localhost:8001
NEXTAUTH_SECRET=test-secret-key-for-local-testing-only-do-not-use-in-prod

# Database Configuration (Docker PostgreSQL)
DATABASE_URL=postgresql://stories_user:test_password@postgres:5432/stories_db
DB_USER=stories_user
DB_PASSWORD=test_password

# Redis Configuration (Docker Redis)
REDIS_URL=redis://:test_password@redis:6379
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=test_password

# Email Service Configuration (disabled for local testing)
EMAIL_SERVICE_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASSWORD=test-password
EMAIL_FROM=noreply@1001stories.local
MAIL_PASSWORD=test-password

# Demo Mode Settings
DEMO_MODE_ENABLED=true
DEMO_BYPASS_EMAIL=true
DEMO_ACCOUNTS_ENABLED=true
NEXT_PUBLIC_DEMO_MODE=true

# Security Settings (relaxed for local testing)
SECURE_COOKIES=false
CSRF_PROTECTION=false
DISABLE_CSRF_CHECK=true
ENABLE_RATE_LIMITING=false
LOGIN_ATTEMPT_LIMIT=100
LOGIN_WINDOW_MS=900000
ENABLE_AUDIT_LOGGING=false
LOG_FAILED_ATTEMPTS=false

# PgAdmin Configuration
PGADMIN_EMAIL=admin@1001stories.local
PGADMIN_PASSWORD=admin123

# Upstage AI Configuration
UPSTAGE_API_KEY=up_kcU1IMWm9wcC1rqplsIFMsEeqlUXN
UPSTAGE_BASE_URL=https://api.upstage.ai/v1
UPSTAGE_MODEL=solar-pro2